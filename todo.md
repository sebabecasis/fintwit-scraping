# Next

The implemented slice collects ticker mentions, scores sentiment and conviction, writes weekly metrics, and produces a dashboard plus an email summary.

Theme comparison reads the live database. A separate read-only analyst chat is planned and is not part of this repository.
