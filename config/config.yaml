list_id: "${X_LIST_ID}"
username: "${X_USERNAME}"

soft_cap_pages: 5
roster_shrink_halt_pct: 50
new_ticker_lookback_weeks: 8
api_cost_alert_usd: 5

paths:
  db: data/fintwit.db
  tickers_whitelist: config/tickers.txt
  tickers_blacklist: config/tickers_blacklist.txt
  reports: reports/
  dashboard: dashboard/
  logs: logs/
